---
created: 2026-08-31T11:20
updated: 2026-08-31T11:21
---

完了しました。全テスト 44 件(うち milestone 17 件)成功です。

変更内容:

1. P1 旧 boolean 値の正規化 — migrations/versions/e0fbbca5c733_v0_07.py(未適用)の upgrade に、v0.06 の型変更で残った 'true'→'open' / 'false'→'closed' の UPDATE を追加。downgrade には逆変換を追加。マイグレーション実行自体は従来どおりユーザー本人が実施してください。

2. 一覧の完全一致化 — app/routers/timetable.py の /milestone/all を status != closed から status.in_([open, waiting]) に変更し、正規化漏れの旧値が公開一覧に流れないようにしました。

3. _next_color の waiting 対応 — 色選定の「使用中」判定を open のみ → open + waiting に変更。waiting 中の色が解放されて新規マイルス トーンと被る問題を解消。

4. ハンドラ改名 — close_milestone → update_milestone(ルートは /milestone/update/{id} のまま変わりません)。

5. P2 テスト復元 — tests/test_milestone.py に以下を追加:
   - test_milestone_update_after_closed_returns_409: remove で closed 化後、title 編集 / accomplished_date あり / 明示 null の 3 パ ターンすべて 409 を検証
   - test_next_color_counts_waiting_as_used: waiting 中の色が再使用されないことを検証
   - waiting 再指定 200 の既存テスト(#6)は別テストとして残しています

6. ルール更新 — .hermes/rules/milestones.md の色ルールと /milestone/all 契約を実装に合わせて更新。

なお /milestone/remove は「論理 close(status=closed)」が requirement-03 の契約(子イベント completed=True を返す)なので物理削除には変えず、現契約を維持しました。物理削除にしたい場合は要件側の変更が必要です。

