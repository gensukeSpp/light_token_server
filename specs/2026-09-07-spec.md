---
created: 2026-09-07T09:52
updated: 2026-09-07T09:54
---

変更を適用しました。

app/routers/timetable.py の update_event_item で、milestone_id の判定を is not None から model_fields_set に変更しました（update_milestone の accomplished_date と同じ前例パターン）。これで明示的な null 送信時に「所属なし」(milestone_id=None) として保存されます。スキーマ変更は不要でした。

検証: uv run pytest → 44 passed。既存テストはすべて通っています。

なお、Pyright の既知の誤検知（Column 型への代入警告、メモリに記録済みの内容と同じ）は出ていますが、ユーザーの方針通り無視して問題あ りません。

要件上、milestone_id を削除したイベントはデフォルト色 (#2196f3) に戻る規則があります（.hermes/rules/milestones.md:28）が、現コードでは色変更ロジックが未実装のため、必要であれば別途対応してください。
