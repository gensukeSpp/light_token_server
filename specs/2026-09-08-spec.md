---
created: 2026-09-08T09:53
updated: 2026-09-08T10:15
---


Task-11 の実装が完了しました。5 コミット、全テスト 50 件 GREEN です。

実装内容

Task 1 — 依存・設定 (836da2e)
- pyproject.toml: apscheduler>=3.10.0 追加(導入されたのは 3.11.3)
- app/config.py: MILESTONE_CLOSE_GRACE_DAYS(デフォルト5)/ MILESTONE_CLOSE_INTERVAL_MINUTES(デフォルト60)/ ENABLE_MILESTONE_SCHEDULER(デフォルト true)を env 対応で追加

Task 2 — ジョブ本体 (8c8ca65)
- app/jobs/close_milestones.py: 純粋関数 close_expired_waiting_milestones(db, today) -> int(条件: status == waiting かつ accomplished_date + 5日 <= today、境界は決定済みの <= 採用)と、SessionLocal を使う execute_close_job()

Task 3 — スケジューラ (c47f0d1)
- app/scheduler.py: BackgroundScheduler(timezone="Asia/Tokyo") + IntervalTrigger でジョブ登録
- app/main.py: lifespan で起動/停止。ENABLE_MILESTONE_SCHEDULER=false 時は起動しない
- 実検証: ENABLE_MILESTONE_SCHEDULER=false uvicorn app.main:app で起動 → /health 200 OK 確認済み

Task 4 — テスト (b8cd51f)
- tests/test_milestone.py に 6 件追加: 猶予内は不変 / 猶予超過で closed 化 / open・closed は対象外 / 子イベント completed 不変 / 境 界(±4日 猶予内、−5日 当日確定)/ accomplished_date=None は対象外
- today を注入するため DB 直操作ヘルパーで accomplished_date を調整する方式
- 既存 16 件を含む 23 件全パス。全体でも 50 passed

Task 5 — ドキュメント (d331b53)
- requirement-03.md に 2026/09/08 追記として自動 closed の仕様を反映

補足
- 子イベント completed は今回スコープ外(決定事項 3)。次のタスクで対応
- 手動 QA 手順は tasks/task-11/test-plan.md のとおり(MILESTONE_CLOSE_GRACE_DAYS=0 + MILESTONE_CLOSE_INTERVAL_MINUTES=1 で猶予確認で きます) -> 確認しました。
- ~~tasks/task-11/ は gitignore 対象のため未コミットのままです~~

---

.hermes/rules/milestones.md に反映しました。2 箇所の変更です。

1. Semantic rules: closed の発生経路を「/milestone/remove の即時 closed」に加えて「waiting からの自動 closed(Task-11)」の 2 経路であることを明記。旧記述の「/milestone/remove のみ generated」を解消。

2. API endpoints: 自動 closed の項目を新設。
   - 猶予日数 MILESTONE_CLOSE_GRACE_DAYS(デフォルト 5 日、境界 <= で当日確定)
   - 実行間隔 MILESTONE_CLOSE_INTERVAL_MINUTES / 無効化フラグ ENABLE_MILESTONE_SCHEDULER=false
   - 実装箇所(app/jobs/close_milestones.py、app/scheduler.py、lifespan)
   - 子イベント completed は不変(次タスクで整合対応)
   - closed 後は /milestone/all から自動で外れ、イベント色がデフォルトに戻る(フロント変更不要)

「waiting から re-open 可」「closed 後の再 update は 409 拒否」など既存契約の記述は今回の変更で影響を受けないため、そのまま維持して います。


