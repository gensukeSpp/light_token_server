---
created: 2026-08-27T10:40
updated: 2026-08-27T10:45
---

マイルストーン status の Boolean→String(10) 移行が完了しました。テストも全37件パスしています。

変更内容

app/models.py
- MILESTONE_OPEN="open" / MILESTONE_WAITING="waiting" / MILESTONE_CLOSED="closed" の定数を追加
- MilestoneORM.status を Column(Boolean, server_default=expression.true()) → Column(String(10), server_default=MILESTONE_OPEN) に変 更（デフォルト "open"）
  - ※ Boolean/expression import は DISPLAY/ADMIN/completed でまだ使用中のため残置

app/routers/timetable.py
- _next_color のフィルタ status.is_(True) → status == MILESTONE_OPEN
- /milestone/add の status=True → MILESTONE_OPEN
- /milestone/all の status.is_(True) → status == MILESTONE_OPEN
- /milestone/update/{id} の target.status is False → target.status == MILESTONE_CLOSED、target.status = False → MILESTONE_CLOSED

tests/test_milestone.py — status の検証を is True/is False → "open"/"closed"

documents
- requirement-03.md は既に更新済みでした
- AGENTS.md の Milestones セクション（Table schema の status、Semantic rules、/milestone/all の説明）を文字列方式に反映

検証
- pytest tests/test_milestone.py → 10 passed
- pytest 全体 → 37 passed

DB マイグレーション
これまでどおり手動です。あなたが実行される際、M_MILESTONE.status の型を Boolean→String(10) へ変更する必要があります（既存データの Boolean 値 1/0 は、"open"/"closed" として移すかどうかも要判断）。

なお MILESTONE_WAITING は今回のビジネスロジック（close 判定・色衝突）ではまだ未使用です。「waiting」への遷移ロジックは次段階で追加される想定です。

  