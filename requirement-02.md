---
created: 2026-08-06T09:17
updated: 2026-08-06T14:59
---
## 概要
### その前に、なぜ "02" なのか
すでにホスティングもされている、かつての旧~~勤怠管理~~アプリにリンクを間借りしていたものから、  **time-table-to-line** (ローカルでの相対パス `cd ../time-table-to-line`) のアクセストークンを生成する機能を抜粋したことが始まります。 つまり、`requirement-01` は、先に制作を始めた time-table-to-line アプリの要件定義書です。
いつ、旧アプリが閉鎖されても良いように、当アプリを制作します。

### 目的
time-table-to-line に、 **限定されたユーザーのみ** がアクセスできるよう、 **ログイン機能** を備えた、安全なアクセストークン処理をしてもらうバックエンド、また、 **API エンドポイント** アプリです。

## 機能要件
DB モデルは、今のところ **旧アプリの形式** に沿うものとします。
- ログイン機能
- 適切にアクセストークンの発行、破棄、再発行が行われること
- httpOnly セッションクッキーを発行する(上記とは or 条件になる?)
- time-table-to-line アプリへのリンク
- CRUD エンドポイント

### ユーザーストーリー
1. スタッフID、パスワードでサインイン
2. リンクページへ飛ぶ
3. リンクから time-table-to-line に遷移

## 実装要件
- Flask(旧アプリ仕様) -> **FastAPI**
- MySQL -> **PostgreSQL** (Render にホスティングする予定、Noen 連携で永続化)
- Pydantic による、とりわけエンドポイントに対するデータ構造化
- テンプレートファイルの Bootstrap は使わない -> htmx
- 余計な print 、適切でないコメントを排除する

## 実装詳細
1. `app/` 以下、 `login.py`  `form.py`(これは必要なくなるかもしれない) を FastAPI 仕様に変更する
2. リンクページとなる、仮のログイン成功ページを用意しておく(`login.py` にある `select_links` はいらない)
3. 各必要なライブラリをインストール
4. ログインを確認
5. DB マイグレーション <- PostgreSQL への変更に伴い、 **ここは私が** 行う
6. 改めて、ログインを確認
7. `routes.py` `auth_middleware.py` も同様に変更する
8. time-table-to-line へのリンクを貼り、遷移を確認
9. 必要なリファクタリング(クラス名、メソッド名の変更も OK)

**留意点**:
- ライブラリをインストール後、実装した内容に応じて、 随時 `tests/` で、 **テスト** を行ってください。
- 1~7 の **順番は絶対ではありません** 。都合により変更して構いません(9 リファクタリングは随時でも)。 あくまで Goal は「目的」に記した内容です。

### 主なライブラリ
Python 3.13.11
- FastAPI, uvicorn, sqlalchemy, alembic, jinja2, httpx2, Pydantic, pytest ほか
- ライブラリの管理は **uv コマンド** で行います。


